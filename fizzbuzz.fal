#!/usr/bin/env falcon

// Language:       Falcon
// Web site:       http://falconpl.org
// Last tested on: Ubuntu 22.04.5
// Requires:       Build from source, Falcon-0.9.6.8.tgz
//                 (There doesn't seem to be be a falconpl package for Ubuntu 17.10)

for i in [1:101]
    if i % 15 == 0
        > "FizzBuzz"
    elif i % 3 == 0
        > "Fizz"
    elif i % 5 == 0
        > "Buzz"
    else
        > i
    end
end
